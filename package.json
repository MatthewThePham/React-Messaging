{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^2.2.0"
  },
  "scripts": {
    "start": "node index.js",
    "client": "yarn start --prefix client",
    "heroku-postbuild": "yarn add --prefix client && yarn run build --prefix client"
  }
}
